import { makeStyles } from '@material-ui/core/styles';

export const langLoginBarStyles = makeStyles({
  root: {
    background: 'aqua',
    border: '1px solid red',
    padding: '1rem',
    height: '10vh',
    display: 'block',
  },
});
