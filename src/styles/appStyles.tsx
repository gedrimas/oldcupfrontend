import { makeStyles } from '@material-ui/core/styles';

export const appStyles = makeStyles({
  root: {
    background: 'yellow',
    border: '1px solid red',
    padding: '1rem',
    minHeight: '100vh',
  },
});
